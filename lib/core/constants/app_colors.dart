import 'package:flutter/material.dart';

class AppColors {
  // Primary Color Scheme
  static const Color primary = Color(0xFFFFC107); // Yellow
  static const Color primaryContainer = Color(0xFFFF5722); // Orange/Red

  // Secondary & Backgrounds
  static const Color secondary = Color(0xFFFFFFFF); // White
  static const Color secondaryContainer = Color(0xFF000000); // Black
  static const Color surface = Color(0xFFFFFFFF);

  // Text Colors
  static const Color onPrimary = Color(0xFF000000); // Black
  static const Color onSecondary = Color(0xFF000000);
  static const Color onSurface = Color(0xFF000000);
  static const Color onError = Colors.white;


  // Button Colors
  static const Color buttonBackground = primaryContainer;
  static const Color buttonForeground = Colors.white;

    // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Transparent
  static const Color transparent = Colors.transparent;

    // Backgrounds
  static const Color background = Color(0xFFF5F5F5);
  static const Color scaffoldBackground = Color(0xFFFFFFFF);
}
